<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="favorite">


	
	
	<select id="favoriteList" resultType="favoriteDto">
		select * from favorite
		order by movieSeq asc
	</select>
	
	
	<insert id="insert" parameterType="favoriteDto">
		INSERT INTO favorite
		(num, id, movieId, movieSeq, repRlsDate, genre, title, poster, isDeleted)
		VALUES(Favorite_Seq.NEXTVAL, 
		#{id}, 
		#{movieId}, 
		#{movieSeq}, #{repRlsDate}, #{genre}, #{title}, 
		#{poster}, 
		#{isDeleted}='no' )
	
	</insert>
	
	
	<select id="getData" parameterType="String" resultType="String">
		SELECT movieId,movieSeq
		FROM favorite
		WHERE id=#{id}
	</select>
	
	<select id="getCount" parameterType="String" resultType="int">
		SELECT count(movieId)
		FROM favorite
		WHERE id=#{id}
	</select>
	
	<update id="delete" parameterType="String">
		UPDATE favorite
		SET isDeleted='true'
		WHERE id=#{id, jdbcType=VARCHAR}
	</update>
	
</mapper>